import { NovaButtonComponent } from './button.component';

describe('NovaButtonComponent', () => {
  it('should create instance', () => {
    const component = new NovaButtonComponent();
    expect(component).toBeTruthy();
  });
});
