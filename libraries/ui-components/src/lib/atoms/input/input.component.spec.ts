import { NovaInputComponent } from './input.component';

describe('NovaInputComponent', () => {
  it('should create instance', () => {
    const component = new NovaInputComponent();
    expect(component).toBeTruthy();
  });
});
